{
  "name": "{{PACKAGE_NAME}}",
  "description": "Symfony bundle: {{VENDOR}}{{BUNDLE}}Bundle",
  "type": "symfony-bundle",
  "license": "proprietary",
  "keywords": ["symfony", "bundle", "{{BUNDLE_SLUG}}"],
  "authors": [
    { "name": "Florimond Jouffroy", "email": "florimond@example.com" }
  ],
  "minimum-stability": "stable",
  "prefer-stable": true,
  "require": {
    "php": ">=8.4",
    "symfony/config": "^7.4",
    "symfony/dependency-injection": "^7.4",
    "symfony/http-kernel": "^7.4"
  },
  "autoload": {
    "psr-4": {
      "{{NAMESPACE_ESCAPED}}\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "{{NAMESPACE_ESCAPED}}\\Tests\\": "tests/"
    }
  }
}